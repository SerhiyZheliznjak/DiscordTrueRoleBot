import  from '../../src/';

describe(``, () => {
    let testedStuff;
    beforeEach(() => {
        setupTestData();
        setupMocks();
        testedStuff = new ();
    });

    it(`should `, () => {

    });
    it(`should `, () => {

    });
    it(`should `, () => {

    });

    function setupMocks() {

    }

    function setupTestData() {
        process.env.MONGODB_URI = 'Mongo/db';
    }
});
